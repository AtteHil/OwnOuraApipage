const mongoose = require("mongoose");
// this schema might not bee needed
const Schema = mongoose.Schema;
let sleepDataSchema = new Schema({
    
    
});
module.exports = mongoose.model("category", categorySchema);